server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_health_system?serverTimezone=Asia/Shanghai&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: xutao0218@
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    host: smtp.qq.com
    username: 3482803488@qq.com
    password: mcdoowsltrbecifa
    port: 587
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: false
            trust: smtp.qq.com
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
        debug: true
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false

security:
  jwt:
    # 至少 32 位随机字符串，生产环境请使用强度更高的值并放到环境变量/配置中心
    secret: change-me-please-change-me-please-123456
    # 过期时间（小时），默认 7 天
    expiration-hours: 168
